# This variable defines the base URL for your Spring Boot backend API.
# The React app will read this value to know where to send its requests.
# Note: In Vite, environment variables must start with VITE_ to be exposed to the client-side code.

VITE_API_BASE_URL=http://localhost:8081/api
